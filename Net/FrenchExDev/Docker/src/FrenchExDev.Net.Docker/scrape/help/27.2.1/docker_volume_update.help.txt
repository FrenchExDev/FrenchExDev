
Usage:  docker volume update [OPTIONS] [VOLUME]

Update a volume (cluster volumes only)

Options:
      --availability string   Cluster Volume availability ("active",
                              "pause", "drain") (default "active")
